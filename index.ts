const display = document.getElementById('display')?.innerHTML;
const numeralButton: string = document.querySelectorAll(".numeral");

numeralButton.addEventListener("click", () => {
    console.log('itworks')
});
